# Names only. Copy to .env (gitignored) and fill in. Needed only for the M5 frontier baseline.
# LiteLLM model id of the frontier vision model, e.g. a provider-prefixed id.
FRONTIER_MODEL=
# Set the key that matches the provider of FRONTIER_MODEL.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
